export enum URL {
    PRODUCTS = "/products",
    CATEGORIES = "/categories",
    ORDERS = "/orders",
    USERS = "/users",
    AUTH = "/auth",
    REVIEWS = "/reviews",
    SEARCH = "/q?term=",
    STATISTICS = "/statistics",
    CAROUSEL="/carousel"
}